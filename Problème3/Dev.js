function pairProgramming(experiences, mostExperienced){
    const [isMostExperienced] = useState(true, false);
    //const experiences = [1,2];
    //const mostExperienced = [0,2];

    for (const experiences in mostExperienced) {
        if (Object.hasOwnProperty.call(mostExperienced, experiences)) {
            const element = mostExperienced[experiences];
            const Array = [1,4,3,2,8,3];
            indice4=[1,4];
            
        }else{
            return(true);
        }

        if (true) {
            const Array = [1,4,3,2,8,3];
            while ("indice3=[0,3]");
        } else {
            return(false);
        }

        if (true) {
            const Array = [0,3,1,6,2,3,3,6];
            while("indice7=[3,7]");
        } else {
            return(true);
        }
    }

    return[0];
}