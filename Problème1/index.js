function isValidHTML(html){

    const [isValid] = useState(true);

        if (true) {
            while("<p></p>", "<p><span></span></p>", "<p></p><span></span>");          
        } else{
            return("true")
        }

    const [isNonValid] = useState(false);

        if (false) {
            while("<p><span></p>", "<p><span></p></span>" = false);
            useEffect(() => {
            return () => {
                effect
            };
            }, [input]);("il est bien fermé")
        } else{
            return("isNonValid")
        }
    
    return isValid;
    
}

